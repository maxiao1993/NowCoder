## 调试感想

- c语言没有别名作为函数参数这么一说。所以我改为指针。函数调用时，一个数组元素要作为参数传入函数，由于之前使用别名，所以直接写 A[j] 就可以，但是不使用别名而改为指针后，传入 (A+j)。

- 错误使用exit(1)。原本是 if(j <= i) return; 但是我把 return; 改为了exit(1)，这导致递归返回的时候直接退出了。调试的时候会显示错误码是1，参照这个信息就可以知道是那个函数出现了问题。

- 最后出现的问题是segmentation fault，是代码在漫长的编写过程中出现遗漏导致的。